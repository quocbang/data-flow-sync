syntax = "proto3";

package function;
option go_package = "github.com/quocbang/data-flow-sync/server/utils/function";

enum FuncName {
    NONE = 0;

    // limiatry hour
    UPLOAD_LIMITARY_HOUR = 1;
    
    // station
    CREATE_STATION_MERGE_REQUEST = 2;
    GET_STATION_MERGE_REQUEST = 3;

    // station group

    // add more here
}